{
  "name": "url-shorty-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "URL shortener backend API (Express, JSON persistence).",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}